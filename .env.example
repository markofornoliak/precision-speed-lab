# Listener
HOST=0.0.0.0
PORT=3000
NODE_ENV=production

# Measurement node identity. Useful when several regional nodes are deployed.
MEASUREMENT_NODE_ID=fra-1
MEASUREMENT_REGION=eu-central
PUBLIC_BASE_URL=https://fra.example.net

# Maximum bytes accepted by one upload/download request (500 MiB default).
MAX_TRANSFER_BYTES=524288000
DOWNLOAD_CHUNK_BYTES=262144
RANDOM_POOL_BYTES=4194304

# Concurrency and abuse protection. In-memory and per process by design.
MAX_ACTIVE_TRANSFERS=128
MAX_ACTIVE_PER_CLIENT=16
RATE_WINDOW_MS=60000
MAX_REQUESTS_PER_WINDOW=900
MAX_BYTES_PER_WINDOW=8589934592
UPLOAD_IDLE_TIMEOUT_MS=30000

# Set only behind a trusted reverse proxy/load balancer that overwrites X-Forwarded-For.
TRUST_PROXY=0

# Same-origin needs no CORS setting. For split frontend/nodes, use a comma-separated allowlist.
CORS_ORIGINS=

# Optional regional nodes exposed by /api/servers.
# Example: [{"id":"fra-1","region":"eu-central","url":"https://fra.example.net"},{"id":"iad-1","region":"us-east","url":"https://iad.example.net"}]
MEASUREMENT_SERVERS_JSON=

# Prometheus text endpoint. Disabled by default. When enabled publicly, set a token.
ENABLE_METRICS=0
METRICS_TOKEN=

# debug also logs high-frequency ping/progress requests; info avoids measurement-side log noise.
LOG_LEVEL=info
